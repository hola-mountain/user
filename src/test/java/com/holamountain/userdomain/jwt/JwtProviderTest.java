package com.holamountain.userdomain.jwt;

import io.jsonwebtoken.io.Encoder;

import io.jsonwebtoken.io.Decoders;
import org.apache.commons.codec.binary.Base64;
import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.*;

class JwtProviderTest {


    @Test
    public void getBase64EncodeSecretKey() {
//        String text = "holam_project_jwt_secret_key"; /* base64 encoding */
//        byte[] encodedBytes = .encodeBase64(text.getBytes()); /* base64 decoding */
//        byte[] decodedBytes = Base64.decodeBase64(encodedBytes);
//        System.out.println("인코딩 전 : " + text);
//        System.out.println("인코딩 text : " + new String(encodedBytes));
//        System.out.println("디코딩 text : " + new String(decodedBytes));
    }
}